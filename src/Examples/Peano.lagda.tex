\begin{code}
module Examples.Peano where
\end{code}

%<*Peano>
\begin{code}
data ℕ : Set where
    zero : ℕ
    suc : ℕ → ℕ

data _≤_ : ℕ → ℕ → Set where
    z≤n : {n : ℕ} → zero ≤ n
    s≤s : {n m : ℕ} → n ≤ m → suc n ≤ suc m
\end{code}
%</Peano>
